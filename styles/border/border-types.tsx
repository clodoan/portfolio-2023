export type BorderRadius = {
  none: string;
  small: string;
  medium: string;
  large: string;
  full: string;
};

export type BorderWidth = {
  none: string;
  small: string;
  medium: string;
  large: string;
};

export type Border = {
  radius: BorderRadius;
  width: BorderWidth;
};
